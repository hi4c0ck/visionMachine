node -e "const fs=require('fs'); let c=fs.readFileSync('D:/work/horizonsMachine/VisionMachine/src/components/Workspace.svelte','utf8'); c=c.replace('let selectedSession = \$derived(', 'let selectedSession = \$derived.by('); c=c.replace(/let selectedSession = \$derived\.by\(\(\) => \{\n\t\tif \(!selectedProject \|\| !selectedSessionId\) return null;\n\t\tconst sess = selectedProject\.sessions\.find\(s => s\.id === selectedSessionId\);\n\t\treturn sess \|\| null;\n\t\}\n\t\);/, 'let selectedSession = \$derived.by(() => {\n\t\tif (!selectedProject || !selectedSessionId) return null;\n\t\tconst sess = selectedProject.sessions.find(s => s.id === selectedSessionId);\n\t\treturn sess || null;\n\t});'); fs.writeFileSync('D:/work/horizonsMachine/VisionMachine/src/components/Workspace.svelte', c); console.log('Fixed');"