# Deterministic installs: vite-plugin-svelte@6 declares peer vite ^6.3||^7 but
# the project runs vite 8 (installed with legacy peer deps since the app era).
# Without this, a plain `npm install` in CI ERESOLVEs and the lockfile can't
# be regenerated.
legacy-peer-deps=true
